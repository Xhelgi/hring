# Design & Customization

All visual parameters (colors, line strokes, padding) are currently defined in `theme.rs`. The default theme features a clean, semi-transparent monochrome aesthetic with red accents.

*Is planned to move these settings to a persistent JSON configuration file.*